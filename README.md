1.deploy ec2 in instance in 2 regions with same resource block in config
2.remote backend state in S3. but, config for S3 remains in the same config
3.Hosted a static webpage in the instances using Nginx server.
4.Mounted an s3 bucket to the EC2 instances that created earlier using IAM roles and policies.
5.Implemented a cross-region bucket replication using Two S3 buckets that are provisioned in different regions.
6.Hosted a static webpage in the instance using null-resource and provisioners.
